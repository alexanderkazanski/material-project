import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "hsl(210deg 100% 45.1%)",
      light: "skyblue"
    },
    secondary: {
      main: "#15c630"
    },
    otherColor: {
      main: "#999"
    }
  }
})